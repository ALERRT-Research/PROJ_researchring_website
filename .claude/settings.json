{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json; d=json.load(sys.stdin); cmd=d.get('command',''); (sys.stdout.write('PUSH BLOCKED — project policy requires explicit user approval before any git push. Present the completed changes to the user and wait for their confirmation in a separate response. No exceptions.\\n'), sys.exit(2)) if 'git push' in cmd else None\""
          }
        ]
      }
    ]
  }
}
